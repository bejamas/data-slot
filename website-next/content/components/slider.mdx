---
title: Slider
description: A control for choosing a numeric value or range by dragging a thumb.
---

<PackageInfo name="slider" />

```bash
npm install @data-slot/slider
```

## Anatomy

The thumb count defines the slider: one thumb for a single value, two for a range. `slider-control` is an optional wrapper for when you need another box to style.

```html
<!-- Single value slider -->
<div data-slot="slider" data-default-value="50">
  <div data-slot="slider-track">
    <div data-slot="slider-range"></div>
  </div>
  <div data-slot="slider-thumb"></div>
</div>

<!-- Optional control wrapper -->
<div data-slot="slider" data-default-value="50">
  <div data-slot="slider-control">
    <div data-slot="slider-track">
      <div data-slot="slider-range"></div>
    </div>
    <div data-slot="slider-thumb"></div>
  </div>
</div>

<!-- Range slider (two thumbs) -->
<div data-slot="slider" data-default-value="25,75">
  <div data-slot="slider-track">
    <div data-slot="slider-range"></div>
  </div>
  <div data-slot="slider-thumb"></div>
  <div data-slot="slider-thumb"></div>
</div>
```

## Examples

### Drag to set a value

<Example name="slider" />

### Disabled state

`data-disabled` keeps the current value visible but blocks dragging and keyboard changes.

<Example name="slider" variant="extra" />

## API reference

<include>../_generated/slider-api.md</include>
